/*const assert = require('assert');
const { getNextCommand } = require('./utils');

describe('You', () => {
  let nextCommand;
  before(async () => {
		nextCommand = await getNextCommand(4);
  });

  it('should exit Nano after opening "castle.sh"', async () => {
    assert(nextCommand[0] === 'nano' && /castle\.sh$/.test(nextCommand[1]));
  });
});*/
